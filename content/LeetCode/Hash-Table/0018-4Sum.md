---
title: 18. 4Sum
tags:
  - leetcode
  - Hash_Table
difficulty: Medium
status: reviewing
---

🔗 [**題目連結**](https://leetcode.com/problems/4sum/)

### 📘 先嘗試自己寫

不會寫

---

### 📹 看影片後

---

### 🧠 核心思路

其實就是在[[0015-3Sum]]外再套一層 `for` 迴圈
在 [[0015-3Sum]] 的基礎上在 `i` 前面再加一個指針 `k`，但因為題目不是求等於 `0`，是等於 `target`，所以還要多上剪枝、去重。
 
```cpp
if(nums[k] > target && nums[k] > 0){ // 剪枝，註解 1
    break;
}
if(k > 0 && nus[k] == nums[k-1]){ // 去重複
    continue;
}
for(i = k+1 ; i < nums.size() ; i++){
    if(nums[k] + nums[i] > target && nums[k] + nums[i] > 0 && target > 0){ // 剪枝，註解 1
        break;
    }
    if(i > k+1 && nums[i] == nums[i-1]){ // 去重複
        continue;
    }
    // 以下就是三數之和中 left、right 指針的運用
}
```

> [!note]- 註解
> - 註解 1：
> 	- 假如 `{-4, -1, 0, 0}, target = -5`，的情況下如果我悶單純只檢查 `nums[k] > target`，那就會錯過組合。 `k` 是本題解法最小的指針，在 `nums[k]` 跟 `target` 都是正數的情況，如果都已經大於 `target` 的話，那就不可能會有四元組了。
> 	- 如果 `nums[k] > 0` 就是要處理負數的情況，如果 `target = -10` 此時 `nums[k] = -5` 如果因為條件只有 `nums[k] > target` 就篩掉的話就可能會少了像是 `[-5, -3, -2, 0]`  這種四元組。

> [!example]- **解法實作**
> ```cpp
> class Solution {
> public:
>     vector<vector<int>> fourSum(vector<int>& nums, int target) {
>         vector<vector<int>> result;
>         sort(nums.begin(), nums.end());
> 
>         for(int k = 0; k < nums.size(); k++){
>             if(nums[k] > target && nums[k] > 0){
>                 break;
>             }
> 
>             if(k > 0 && nums[k] == nums[k-1]){
>                 continue;
>             }
> 
>             for(int i = k+1; i < nums.size(); i++){
>                 if((nums[k]+nums[i]) > target && (nums[k]+nums[i]) > 0){
>                     break;
>                 }
> 
>                 if(i > k+1 && nums[i] == nums[i-1]){
>                     continue;
>                 }
> 
>                 int left = i+1;
>                 int right = nums.size()-1;
>                 while(right > left){
>                     if(((long)nums[k]+nums[i]+nums[left]+nums[right]) > target){
>                         right--;
>                     }else if(((long)nums[k]+nums[i]+nums[left]+nums[right]) < target){
>                         left++;
>                     }else{
>                         result.push_back(vector<int>{nums[k], nums[i], nums[left], nums[right]});
>                         
>                         while(right > left && nums[right] == nums[right-1]){
>                             right--;
>                         }
>                         while(right > left && nums[left] == nums[left+1]){
>                             left++;
>                         }
>                         right--;
>                         left++;
>                     }
>                 }
>             }
>         }
>         return result;
>     }
> };
> ```